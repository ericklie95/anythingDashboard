
# Google Calendar API integration
# Requires a valid OAuth 2.0 token. To set up:
#   1. Create a project in Google Cloud Console
#   2. Enable the Google Calendar API
#   3. Create OAuth 2.0 credentials and download the JSON
#   4. Run the auth flow once to generate a token and store it in .env as GOOGLE_CALENDAR_TOKEN


def get_today_events() -> list[dict]:
    # Placeholder — returns empty until OAuth token is configured
    return []
